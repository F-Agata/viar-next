export const handleScrollToSlide01 = (slide01Ref) => {
  slide01Ref.current.scrollIntoView({
    behavior: 'smooth',
    block: 'nearest',
    inline: 'center',
  })
}

export const handleScrollToSlide02 = (slide02Ref) => {
  slide02Ref.current.scrollIntoView({
    behavior: 'smooth',
    block: 'nearest',
    inline: 'center',
  })
}

export const handleScrollToSlide03 = (slide03Ref) => {
  slide03Ref.current.scrollIntoView({
    behavior: 'smooth',
    block: 'nearest',
    inline: 'center',
  })
}
